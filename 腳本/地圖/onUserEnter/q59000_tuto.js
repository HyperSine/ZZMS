/* global ms */

function action(mode, type, selection) {
    ms.showInstruction("任務快捷鍵,按下#e#b[Q]#k#n 可開啟或關閉任務視窗.", 150, 5);
    ms.showInstruction("#e#b[←],[→]#k#n 按下方向鍵即可移動", 150, 5);
    ms.dispose();
}